services:
  - type: web
    name: HaiEmetWeb
    env: node
    buildCommand: "npm install"
    startCommand: "node render-server.js"

  - type: worker
    name: HaiEmetBot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
```

---

## **📋 איפה להכניס בGitHub:**
```
HaiEmetCoreAi repo:
├─ render-server.js ← קובץ 1
├─ package.json ← קובץ 3
├─ main.py ← קובץ 4
├─ requirements.txt ← קובץ 5
├─ render.yaml ← קובץ 6
└─ public/
   └─ index.html ← קובץ 2
